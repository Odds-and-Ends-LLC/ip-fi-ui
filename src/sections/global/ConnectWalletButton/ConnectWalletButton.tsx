// packages
import { Button } from "@mui/material";

export default function ConnectWalletButton() {
  return(
    <Button fullWidth variant="solidGreen" onClick={() => {}}>
      CONNECT
    </Button>
  )
}
